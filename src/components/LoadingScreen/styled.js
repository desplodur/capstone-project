import styled from 'styled-components';

const StyledLoadingScreen = styled.article`
	display: flex;
	align-items: center;
	justify-content: center;
	width: 100vw;
	height: 100vh;
	background-color: var(--blue);
	color: var(--light);
	font-size: 2em;
`;

export default StyledLoadingScreen;
